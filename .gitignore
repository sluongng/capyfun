/target
**/*.rs.bk
Cargo.lock.orig

# Local API credentials for agent-run / run-agents.sh (never commit secrets).
secrets.env
.env
.env.local

# Claude Code worktrees/session state
.claude/

# Project instructions, kept local only (purged from history).
CLAUDE.md
