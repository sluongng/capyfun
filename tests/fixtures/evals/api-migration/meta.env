KIND="import"
DESC="Upstream renamed the client constructor (Connect -> New); a caller in the package was left un-migrated and no longer compiles. The agent migrates the call sites."
LABEL="//third_party/greeter:greeter"
REPO="acme/greeter"
DEST="third_party/greeter"
AGENT="porter"
AGENT_DESC="codex + gpt-5.5 (fixture/mock)"
VERIFY="go test ./..."
RETRIES="1"
EXPECT="pass"
