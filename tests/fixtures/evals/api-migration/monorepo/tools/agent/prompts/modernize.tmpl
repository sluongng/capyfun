You are modernizing an imported Go package to satisfy its updated test contract.

Changed files: {{changed_files}}
Incoming diff:
{{incoming_diff}}

Implement the missing/updated helper so that `go test ./...` passes.
