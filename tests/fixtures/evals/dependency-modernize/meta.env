KIND="import"
DESC="An imported generics helper was dropped; the updated test pins a new Last[T]() contract. The agent reimplements it; its first attempt has a bug the verifier catches, and the failure is fed back so the retry fixes it."
LABEL="//third_party/slices:slices"
REPO="acme/slices"
DEST="third_party/slices"
AGENT="modernizer"
AGENT_DESC="claude_code + opus-4.8 (fixture/mock)"
VERIFY="go test ./..."
RETRIES="1"
EXPECT="pass"
