# Root SRC for the demo monorepo: the workspace anchor.
#
# Sub-package SRC files under third_party/ are generated by `capyfun gen-go`
# from go.mod, then populated with real upstream history by `capyfun import`.

monorepo(
    name = "demo",
    default_branch = "main",
)
